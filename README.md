# mini_gra

Prosty projekt z przykładowymi plikami.

## Kalkulator

Skrypt `calculator.py` pozwala wykonać podstawowe działania matematyczne.

Przykład użycia:

```bash
python calculator.py 2 + 2
```

Działania obsługiwane to dodawanie (`+`), odejmowanie (`-`), mnożenie (`*`) i dzielenie (`/`).

